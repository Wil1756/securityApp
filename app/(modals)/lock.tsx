import { View, Text } from 'react-native';

export default function Page() {
    return (
        <View style={{flex: 1, backgroundColor: '#fff'}}>
            <Text>Page</Text>
        </View>
    );
};